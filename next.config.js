/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["img.ltwebstatic.com", "lh3.googleusercontent.com"],
  },
  // Add other configurations here if needed
};

module.exports = nextConfig;
